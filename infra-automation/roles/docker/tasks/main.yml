- name: Install Docker
  apt:
    name: docker.io
    state: present

- name: Enable Docker
  service:
    name: docker
    enabled: yes
    state: started

- name: Add runner user to docker group
  user:
    name: "{{ ansible_user }}"
    groups: docker
    append: yes